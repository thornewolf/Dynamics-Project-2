\begin{tikzpicture}

  \pgfmathsetmacro{\Gvec}{1.5}
  \pgfmathsetmacro{\myAngle}{20}

  \pgfmathsetmacro{\Gcos}{\Gvec*cos(\myAngle)}
  \pgfmathsetmacro{\Gsin}{\Gvec*sin(\myAngle)}

  \coordinate (centro) at (0,0);
  \draw[dashed,gray,-] (centro) -- ++ (0,-2) node (mary) [black,below]{$ $};
  \draw[double distance = .75mm,line cap=round] (centro) -- ++(270+\myAngle:3) coordinate (bob);
  \pic [draw,->, "$\theta$", angle eccentricity=3] {angle = mary--centro--bob};

  \fill[pattern = north east lines] ($ (centro) + (-1,0) $) rectangle ($ (centro) + (4,0.5) $);
  \draw[thick] ($ (centro) + (-1,0) $) -- ($ (centro) + (4,0) $);
  \draw[thick] (centro) -- ($ (centro) + (0,0.75) $);
  \draw[thick,<->] ($ (centro) + (0,0.65) $) -- ++(3,0) node[black,right]{};
  \draw (1.5,.5) node[label=90:$L$] {};

  \fill[black] (centro) circle (0.075);

  \pgfmathsetmacro{\Gvec}{1.5}
  \pgfmathsetmacro{\myAngle}{30}

  \pgfmathsetmacro{\Gcos}{\Gvec*cos(\myAngle)}
  \pgfmathsetmacro{\Gsin}{\Gvec*sin(\myAngle)}

  \coordinate (centro) at (3,0);
  \draw[dashed,gray,-] (centro) -- ++ (0,-2) node (mary) [black,below]{$ $};
  \draw[double distance = .75mm,line cap=round] (centro) -- ++(270+\myAngle:3) coordinate (bob2);
  \pic [draw,->, "$\phi$", angle eccentricity=3] {angle = mary--centro--bob2};

  % \fill[pattern = north east lines] ($ (centro) + (1,0) $) rectangle ($ (centro) + (0,0.5) $);
  % \draw[thick] ($ (centro) + (-1,0) $) -- ($ (centro) + (1,0) $);
  \fill[black] (centro) circle (0.075);
  \draw[thick] (centro) -- ($ (centro) + (0,0.75) $);

  \draw ($ (bob) + (0,-.05) $) to[R,l=$k$] ($ (bob2) + (0,-.05) $);
  \draw[dashed,gray,-] ($ (bob) + (0,-.05) $) -- ($(bob) + (4,-.05)$);
  \draw [->] ($(bob2) + (0,-.3)$) -- ($(bob2) + (0,-.05)$) {};
  \draw ($(bob2) + (.25,-.1)$) node[label] {$\beta$};

  \draw ($ (bob) + (.1,1.5)$) node[label] {$m,L$};
  \draw ($ (bob2) + (.1,1.5)$) node[label] {$m,L$};

\end{tikzpicture}
